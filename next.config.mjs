const nextConfig = {
  // output: "export",
  // images: {
  //   unoptimized: true, // required for `next export`
  // },
  // images: {
  //   domains: ["drive.google.com"],
  // },
};

export default nextConfig;
